package port

type Service interface {
	//GetLastRates(ctx context.Context, titles []string) ([]Coin, error) // Получить последние цены
	//GetMaxRates(ctx context.Context, titles []string) ([]Coin, error)  // Получить максимальные цены
	//GetMinRates(ctx context.Context, titles []string) ([]Coin, error)  // Получить минимальные цены
	//GetAvgRates(ctx context.Context, titles []string) ([]Coin, error)  // Получить средние цены
}
